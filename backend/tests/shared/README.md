# Shared Backend Tests

Tests in this folder protect rules used by more than one page or feature.

Each shared area must include a README that identifies the owning rule area,
affected pages/features, and where page-level integration coverage lives.

Do not create placeholder shared folders. Add a shared folder only when a
reviewed rule already exists and has tests.

